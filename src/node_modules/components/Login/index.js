import React from 'react'

import s from './_index.scss';

class Login extends React.Component {
  render() {
    return (
      <div className={s.root}>
        {this.props.children}
      </div>
    )
  }
}

export default Login